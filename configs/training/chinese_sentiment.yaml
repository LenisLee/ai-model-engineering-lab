project:
  name: ai-model-engineering-lab

data:
  dataset_name: chnsenticorp
  text_column: text
  label_column: label
  eval_split: validation

model:
  base_model: bert-base-chinese

training:
  output_dir: outputs/checkpoints/chinese_sentiment
  epochs: 1
  batch_size: 8
  learning_rate: 0.00002
