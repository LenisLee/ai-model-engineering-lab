project:
  name: ai-model-engineering-lab

data:
  dataset_name: imdb
  text_column: text
  label_column: label

model:
  base_model: distilbert-base-uncased

training:
  output_dir: outputs/checkpoints
  epochs: 1
  batch_size: 8
  learning_rate: 2e-5
